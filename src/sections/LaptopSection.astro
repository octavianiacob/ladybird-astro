---
import FakeDashboard from "./FakeDashboard.astro";
---

<script>
	import { scaleElementToViewport } from "../utils/helpers";

	const innerEl = document.querySelector(".LaptopSection__inner")!;

	// scaleElementToViewport(innerEl, 1440, 790);
</script>

<section class="LaptopSection__inner">
	<video class="LaptopSection__vid" src="/videos/laptop.mp4"></video>
	<div class="LaptopSection__screen">
		<!-- <canvas></canvas> -->
		<FakeDashboard />
	</div>
</section>

<style lang="scss">
	.LaptopSection {
		&__inner {
			// display: flex;
			// align-items: center;
			// justify-content: center;
			width: 100%;
			height: 100%;
			// background-color: #f0f0f0;
			position: relative;

			// width: 1440px;
			// height: 790px;

			canvas {
				// width: 1440px !important;
				// height: 790px !important;
				position: absolute;
				top: 0;
				left: 0;
			}

			video {
				width: 100%;
				max-height: 100%;
				object-fit: cover;
			}
		}

		&__vid {
			// disable clicking on the video
			pointer-events: none;
		}

		&__screen {
			// width: 53vw;
			// height: 33vw;
			// top: 10.5vw;
			// left: 50vw;
			// transform: translateX(-50%) translateY(0%);

			width: 53vw;
			height: 33vw;
			// background-color: #fff;
			opacity: 0;
			z-index: 1;
			position: absolute;

			width: 53vw;
			height: 33vw;

			top: 4.5vw;
			left: 24vw;
		}
	}
</style>
