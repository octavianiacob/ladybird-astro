---
import SectionLayout from "../layouts/SectionLayout.astro";
import FloatingForm from "../components/FloatingForm.tsx";
export const brandList = [
	{
		name: "Microsoft",
		logo: "/images/microsoft.jpg",
	},
	{
		name: "Cisco",
		logo: "/images/cisco.png",
	},
	{
		name: "Manchester Metroplitan University",
		logo: "/images/machester-metro.svg",
	},
	{
		name: "Health Innovation Manchester",
		logo: "/images/health-innovation.png",
	},
	{
		name: "University of Manchester",
		logo: "/images/university-of-manchester.png",
	},
];
---

<script></script>

<SectionLayout className="BottomSectionWrapper">
	<div class="BottomSection">
		<h1>
			Curious?<br />
			Sign up for all things AI
		</h1>
		<div class="BottomSection__form">
			<FloatingForm client:load />

			<p>
				By joining us, you agree to Ladybird's <a
					href="/files/inCall TnC.pdf"
					target="_blank">Terms of Use</a
				> and <a href="/files/inCall Privacy Policy.pdf" target="_blank"
					>Privacy Policy</a
				>
			</p>
		</div>
		<p class="successMessage">Thank you for subscribing!</p>
		<div class="BottomSection__brands">
			{brandList.map((brand) => <img src={brand.logo} alt={brand.name} />)}
		</div>
	</div>
</SectionLayout>

<style lang="scss">
	.BottomSection {
		width: 100%;
		// height: auto;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 3rem;

		padding: 3rem;

		h1 {
			font-size: 4rem;
			font-weight: 400;
			line-height: 1.2;
			text-align: center;
			margin-top: 3rem;

			// for revealing animation
			// opacity: 0;
			// transform: translateY(40%);
		}

		&__form {
			display: flex;
			flex-direction: column;
			gap: 1rem;
			width: 100%;
			max-width: 500px;
			margin-bottom: 1.5rem;

			color: #bdbdbd;

			// for revealing animation
			// opacity: 0;

			p {
				font-size: 0.8rem;
				color: #666;
				text-align: center;
			}

			a {
				// color: #ff0000;
				text-decoration: none;
				color: #666;

				&:hover {
					text-decoration: underline;
				}
			}

			button {
				width: 100%;
				padding: 1rem;
				border: none;
				border-radius: 0.5rem;
				background-color: #000;
				color: #fff;
				font-size: 1.5rem;
				cursor: pointer;
				transition: background-color 0.3s;

				&:hover {
					background-color: #ff0000;
				}

				span {
					display: inline-block;
				}

				svg {
					animation: spin 1s ease-in-out infinite;
					// visibility: hidden;
					display: none;
				}
			}
		}

		&__brands {
			display: flex;
			align-items: center;
			justify-content: center;
			gap: 2rem;
			flex-wrap: wrap;
			margin-top: 2rem;

			// for revealing animation
			// opacity: 0;

			img {
				// width: 100px;
				height: 100px;
				object-fit: contain;
			}
		}

		.successMessage {
			color: #04db00;
			font-size: 1.5rem;
			font-weight: 700;
			display: none;
		}
	}

	@media (max-width: 768px) {
		.BottomSection {
			padding: 1rem;
			justify-content: center;

			h1 {
				font-size: 1.8rem;
				margin-top: 4rem;
			}

			&__form {
				margin-bottom: 0;
				input {
					font-size: 0.75rem;
					padding: 0.8rem;
				}

				p {
					font-size: 0.6rem;
				}

				button {
					font-size: 0.8rem;
					padding: 0.8rem;
				}
			}

			&__brands {
				margin-top: 0;
				img {
					// height: clamp(0px, 4.5vh, 36px);
					height: clamp(0px, 5.9vh, 43px);
				}
			}
		}
	}
</style>
