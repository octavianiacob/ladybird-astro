---
export interface Props {
	className: string;
}

const { className } = Astro.props;
---

<main class={`SectionLayout ${className}`}>
	<slot />
</main>

<style lang="scss">
	@import "../styles/variables";

	.SectionLayout {
		min-height: 100vh;
		background-color: $main_dark;
		position: relative;
		display: flex;
	}

	* {
		scroll-behavior: smooth;
	}
</style>
